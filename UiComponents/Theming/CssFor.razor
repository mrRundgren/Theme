@namespace UiComponents.Theming
@inherits UiComponents.Core.UiComponentBase

@code {
    [Parameter] public string Name { get; set; } = null!;

    protected override void OnInitialized()
    {
        typeof(ThemeProvider).GetProperty(Name)?.SetValue(Theme, ClassAttribute);
    }

}